path:
  pipeline_output_path: $BEDBASE_DATA_PATH_HOST/
  bedstat_dir: outputs/bedstat_output
  bedbuncher_dir: outputs/bedbuncher_output
  remote_url_base: null
database:
  name: postgres
  user: postgres
  password: bedbasepassword
  host: localhost
  port: 5432
  dialect: postgresql
  driver: psycopg2
server:
  host: 0.0.0.0
  port: 8000